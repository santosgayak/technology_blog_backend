import { BlogFormSubmitDirective } from './blog-form-submit.directive';

describe('BlogFormSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogFormSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
